// Auth state is managed via React Query — no Context needed.
// See: hooks/useCurrentUser.ts  (current user + auth state)
//      hooks/useLogin.ts        (login mutation)
//      hooks/useLogout.ts       (logout mutation)
//      hooks/useRegister.ts     (register mutation)
export {};
